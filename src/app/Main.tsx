import { LetterList } from "../common/components/LetterList/LetterList";

export const Main = () => {
    return (
        <main className="flex-grow">
            <LetterList/>
        </main>
    );
}
